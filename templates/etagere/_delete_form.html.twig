<form method="post" action="{{ path('etagere_delete', {'id': etagere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etagere.id) }}">
    <button class="btn">Delete</button>
</form>
